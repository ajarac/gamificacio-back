import { PingController } from './ping';
import { SchoolController } from './school';
import { AvatarController } from './avatar';

export const CONTROLLERS = [ new PingController(), new SchoolController(), new AvatarController() ];
